#include<bits/stdc++.h>
using namespace std;
typedef long long ll;

int main ()
{
    ios_base::sync_with_stdio(0); cin.tie(0); cout.tie(0);
    ll t,n,m,i;
    cin>>t;
    while (t!=0)
    {
        t--;
        cin>>n;
        cout<<n*n<<"\n";
    }

    return 0;
}